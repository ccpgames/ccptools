"""Here we should place code that is used in multiple submodules so they don't
import from one another, thus enabling us to use any single submodule without
everything else tagging along.

"""
from ._decode import *
